import * as React from "react";

import { Loading, ChartContainer } from "components";

export function ChartLoading() {
  return (
    <ChartContainer>
      <Loading />
    </ChartContainer>
  );
}
